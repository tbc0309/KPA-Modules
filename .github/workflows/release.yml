name: Release module

on:
  push:
    tags:
      - 'font-v*'
      - 'rgb-v*'
  workflow_dispatch:
    inputs:
      module:
        description: Module to build
        required: true
        type: choice
        options: [font, rgb]
      tag:
        description: Release tag, for example font-v1.0.1
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
      - name: Select module and tag
        id: select
        shell: pwsh
        run: |
          if ('${{ github.event_name }}' -eq 'workflow_dispatch') {
            "module=${{ inputs.module }}" >> $env:GITHUB_OUTPUT
            "tag=${{ inputs.tag }}" >> $env:GITHUB_OUTPUT
          } elseif ('${{ github.ref_name }}' -like 'font-v*') {
            "module=font" >> $env:GITHUB_OUTPUT
            "tag=${{ github.ref_name }}" >> $env:GITHUB_OUTPUT
          } else {
            "module=rgb" >> $env:GITHUB_OUTPUT
            "tag=${{ github.ref_name }}" >> $env:GITHUB_OUTPUT
          }
      - name: Build
        shell: pwsh
        run: ./build.ps1 -Module '${{ steps.select.outputs.module }}'
      - name: Publish independent release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $Asset = (Get-ChildItem ./dist/*.zip).FullName
          $Notes = if ('${{ steps.select.outputs.module }}' -eq 'font') { 'FONT_RELEASE_NOTES.md' } else { 'RGB_RELEASE_NOTES.md' }
          gh release create '${{ steps.select.outputs.tag }}' $Asset --notes-file $Notes --title '${{ steps.select.outputs.tag }}'
